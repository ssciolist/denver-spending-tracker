<h1><%= @vendor.name %>, operating in <%= @vendor.state %></h1>

<table>
  <tr>
  <th>Program</th>
    <%= render partial: 'layouts/table_headers' %>
  </tr>
  <% @vendor.purchases.each do |purchase| %>
    <tr>
      <td><%= purchase.show_program.name %></td>
      <td><%= purchase.amount %></td>
      <td><%= purchase.description %></td>
      <td><%= purchase.transaction_date %></td>
      <td><%= purchase.payment_date %></td>
    </tr>
  <% end %>
</table>
